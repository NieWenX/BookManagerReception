spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
  thymeleaf:
    cache: false

mybatis:
  type-aliases-package: com.nie.pojo
  mapper-locations: classpath:com/nie/mapper/*.xml
